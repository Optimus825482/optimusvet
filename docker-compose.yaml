version: "3.8"

services:
  # Next.js Application
  # Uses external PostgreSQL database at 77.42.68.4:5437
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: optimus-vet-app
    environment:
      DATABASE_URL: postgres://postgres:518518Erkan@77.42.68.4:5437/optimusvet
      NEXTAUTH_URL: ${NEXTAUTH_URL:-https://optimus.celilturan.com.tr}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET:-your-secret-key-here}
      NODE_ENV: production
      PORT: 3002
    ports:
      - "3002:3002"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3002/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped

# Note: PostgreSQL service removed - using external database
# Database: postgres://postgres:518518Erkan@77.42.68.4:5437/optimusvet
